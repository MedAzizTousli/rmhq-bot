DISCORD_TOKEN=
# Required for instant guild slash-command updates. Comma-separated IDs are supported.
# GUILD_ID is also accepted.
DISCORD_GUILD_ID=
# Set to 0/false/no to avoid Discord sync rate limits during frequent restarts
SYNC_COMMANDS_ON_STARTUP=1
# Server/channel IDs are configured in config.yaml (per server).
NOTION_TOKEN=
NOTION_DATABASE_ID=
PRIZE_POOL_NOTION_DATABASE_ID=
DATABASE_URL=
SUPABASE_PASSWORD=

